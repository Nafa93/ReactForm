export abstract class ValidationErrors {
  static readonly EMPTY_STRING = "You can't input an empty value";
  static readonly EQUAL_STRING = "Both strings are the same";
  static readonly NOT_CONTAINS_NUMBER = "String doesn't contain a number";
}
